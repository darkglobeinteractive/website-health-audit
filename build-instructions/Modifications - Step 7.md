# Changes in preparation of Paragram enhancements

Here are some changes I'd like performed.

## Change third-party credentials notes field to a textarea

In the tbody#credential-body table body, you made the .credential-notes field an input[type="text"] field. I would like this to be a textarea field with a default height of two lines.

## Change plugin evaluation notes field to a textarea

I have a similar situation here. In the tbody#plugin-tbody table body, the input.plugin-notes is an input[type="text"]. I'd like this to be a textarea field with a default height of two lines.
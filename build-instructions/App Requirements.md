# Basic Technical Requirements

- This should generate a single page HTML page that I can host locally
- Only HTML, CSS and raw JavaScript should be used
- Include the CSS and JavaScript in linked files 
- We will want to be able to print-out the page (or save it out as a PDF) for long-term documentation and delivery to the client
- Bullet points in @build-instructions/Concerns.md should retain nesting and utilize HTML checkboxes
- Each bullet point should have a link with the verbiage "Add Notes" at the end of the text which will insert a textarea within the bullet point for notes
